export default function EditLayout({
    children,
}: Readonly<{children: React.ReactNode}>) {
    return (
        <>
            {/* This layout is for Lexical Editor... */}
            {children}
        </>
    );
}
